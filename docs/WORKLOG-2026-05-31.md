# 워크로그 — 2026-05-31 (인스타 자동화 안정화 + 알림 + 성능)

> 다른 대화/세션이 이어서 작업할 수 있게 정리한 핸드오프.
> 전체 구조도는 **`OVERVIEW.md`**, 이전 핸드오프는 **`HANDOFF.md`** 참고.
> ⚠️ public repo — **토큰·키 값은 여기 안 적음**(위치만). 실제 값은 `.env.local`/Vercel/User 환경변수에.

---

## 1. 이번 세션에 한 것 (커밋 `991f16d`~`4653799`)

### 인스타 로컬 파이프라인 (INGEST)
- **B루틴 헤드리스 안정화** — 핵심 재설계. cmd 배치(`run-b.bat`) → **`run-b.ps1`(PowerShell, 순수 ASCII)**.
  - 구조: **PowerShell이 `--auto-export`/`--import` 실행, Claude는 분류만**(ASCII 임시폴더 `%TEMP%\umbba-b`에서 input.json+RULES.md 읽고 results.json 쓰기, 셸 명령·cd 없음).
  - 헤드리스 함정(자세히는 메모리/주석): ① cmd 파이프 `type|claude` → `0xC000013A`(CONTROL_C) 즉사 ② **콘솔 창 닫으면** claude 죽음 → 작업을 `-WindowStyle Hidden`으로 ③ claude가 CLI 오케스트레이션하면 hang → 파일 분류만 ④ `.ps1` 순수 ASCII(PS5.1 ANSI 오독) ⑤ `Stop-ScheduledTask`가 손자 claude 안 죽임(좀비 PID kill).
  - 인증: **`CLAUDE_CODE_OAUTH_TOKEN`(User 환경변수, `claude setup-token` 발급, 1년)**. claude.exe 실제 경로 = `%LOCALAPPDATA%\Packages\*Claude*\LocalCache\Roaming\Claude\claude-code\<ver>\claude.exe`(run-b.ps1 동적 탐색).
  - 헬퍼: `tools/umbba-cli/setup-token.ps1`(토큰 발급), `smoke-auth.ps1`(인증 확인).
- **부하 분산 스케줄**: A스캔 `--max-accounts 50`, **밤새 4회**(21:30·23:00·00:30·02:00, XML 다중트리거+WakeToRun) → last_scanned_at 순환으로 활성계정(현 202개) 각 1회. B는 **03:00**, run-b.ps1 야간상한 `maxItems 25`(=배치1개, 검증됨). `UMBBA_SLEEP=10`.
- **auto-export 21분 버그 수정**: `_process_export`가 텍스트 모드인데도 항목당 sleep → 124개 21분. `with_images`일 때만 sleep으로 고침(즉시화).
- **첫 이미지 우선**: gallery-dl 다운로드 메인 이미지를 "가장 큰 것"→"첫 장(캐러셀 1번)"으로.
- **네이버 자동수집 보류**: Gemini API 키 정지(CONSUMER_SUSPENDED) → 매일 0건 생산. `/api/cron/ingest`에서 `INGESTION_ENABLED!=="true"`면 skip(기본 OFF, 마감만료 step은 유지).
- 쿠키 백업·로그·exports gitignore 추가. 인스타 스크립트 형상관리 포함.

### 웹 기능 (CURATOR/ANALYTICS)
- **`/admin/queue`**: 카드에 (예상)마감기한 배지(D-day, D-3 이내 빨강).
- **수정 화면**: "수정 저장" 옆 초록 **"발행 저장"** 버튼 — 수정+status를 published로 강제(`updateAndPublishPostAction`).
- **`/admin` 파이프라인 대시보드**: 팔로잉→큐→발행 퍼널 + 최근7일 일별표 + 저조 경고(24h 수집<5). `selectPipelineStats`(instagram_accounts/ingest_queue/posts 집계).
- **텔레그램 일일 현황 알림**: `/api/admin/notify-telegram`(통계+승인대기 집계 후 전송). `ingest.py`가 `--import` 완료 후 트리거. ⚠️ **Vercel env 미설정 시 no-op** → 후속 필요(아래).

### 성능 (INFRA/FRONT)
- **Vercel 함수 리전 서울(`icn1`)**: 기본 미국(iad1)→서울. 동적요청 ~510→~220ms.
- **필터 클라이언트 즉시화**: 메인(DiscoveryView)·관리자(AdminPostsView). 시기/유형/상태/정렬 클릭 시 서버 재요청 0 → 즉시. (검색 q만 서버 FTS 유지)
- **콜드스타트 워밍**: `/api/health` + `.github/workflows/warm-ping.yml`(GitHub Actions 5분마다 curl). public repo Actions 무료.
- **웹 스플래시 번쩍임 제거**: SplashScreen이 SSR 콘텐츠 위에 늦게 덮여 깜빡임 → 웹 생략, PWA만 유지.
- `/my` 로딩 스켈레톤 추가(메인·상세는 기존 존재).

---

## 2. 현재 운영 상태

### 작업 스케줄러 (로컬 PC, Interactive=로그인 필요)
| 작업 | 시각 | 비고 |
|---|---|---|
| `엄빠레이더-스캔` | 21:30·23:00·00:30·02:00 | 회차당 50계정, WakeToRun. 첫 정상 시작 **2026-06-01 21:30** |
| `엄빠레이더-B루틴` | 03:00 | run-b.ps1, 상한25. 첫 정상 **2026-06-02 03:00** |
| `엄빠레이더-백로그` | (1회성, 완료) | 5/31 밤 백로그 124건 소화용. **트리거 끝남 → 삭제해도 됨** |

- **5/31 밤은 정상 사이클 건너뛰고 백로그만** 처리(사용자 요청). 정상 A/B는 6/1 밤부터.
- 야간 운영 조건: PC 밤새 켜짐+로그인(절전은 WakeToRun으로 깨움, 종료/로그오프는 그날 건너뜀), 노트북 전원연결.

### 인증·시크릿 위치 (값 X)
- `CLAUDE_CODE_OAUTH_TOKEN` — Windows User 환경변수 (B루틴 헤드리스 인증)
- `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` — `.env.local`에 있음. **Vercel엔 아직 미등록**(후속)
- `ADMIN_CLI_TOKEN`, `NAVER_*`, `GEMINI_API_KEY`(정지됨), `CRON_SECRET`, `SUPABASE_*` — `.env.local`/Vercel
- 인스타 `cookies.txt` — `tools/umbba-cli/`(gitignore). **만료 시 401 → 주1회 Firefox 재export 필요**

---

## 3. ⚠️ 후속 필요 작업 (다음 대화가 이어서)

1. **[높음] 텔레그램 활성화** — Vercel 환경변수에 `TELEGRAM_BOT_TOKEN`+`TELEGRAM_CHAT_ID`(값은 `.env.local`) 추가 → **Redeploy**. 그 후 `POST /api/admin/notify-telegram`(Bearer `ADMIN_CLI_TOKEN`)로 테스트. (봇 `@umbba_radar_bot` 생성·연결·로컬테스트는 완료)
2. **[정리] `엄빠레이더-B루틴` 트리거 정상화 재확인** — 5/31엔 06-02로 미뤄둠. 6/1 정상 가동되는지, `엄빠레이더-백로그` 임시작업 삭제.
3. **[확인] 첫 정상 야간 사이클(6/1밤~6/2 03:00) 결과** — `tools/umbba-cli/scan-log.txt`(401 없는지), `b-log.txt`, `/admin/queue`(새 카드), 텔레그램 수신.
4. **[결정] 네이버 수집** — Gemini 키 정지로 보류 중. 새 Gemini 무료키 발급 후 Vercel `GEMINI_API_KEY` 교체 + `INGESTION_ENABLED=true`로 부활, 또는 계속 보류(인스타 위주).
5. **[선택] iOS PWA 스플래시** — iOS는 `apple-touch-startup-image` 없어 런치 시 흰 화면. 기기별 스플래시 이미지 생성(`pwa-asset-generator` 등) + `<link>` 태그 추가하면 개선. (Android는 OS 자동 스플래시라 OK)
6. **[선택] Supabase Storage 관리** — 카드 이미지 월 ~100MB 누적(~8~12개월 1GB). 반려(삭제) 카드 이미지가 orphan으로 남음 → 정리 작업 또는 soft-delete 도입 고려.
7. **[선택] `published_at` 컬럼** — 현재 일별 "발행"은 created_at 기준(생성일). 승인일 기준 정확 집계 원하면 컬럼 추가 + approve 액션에서 set.

---

## 4. 참고
- 전체 수집·자동화 지도: `OVERVIEW.md`
- 직전 핸드오프(Phase 2): `HANDOFF.md`
- 분류 규칙: `tools/umbba-cli/RULES.md` / 모듈 오너십: `OWNERSHIP.md`
- 이번 세션 커밋 19개: `991f16d`(네이버보류) ~ `4653799`(웹스플래시).
